排序算法：https://www.cnblogs.com/onepixel/articles/7674659.html
1. O(n^2)
    1. 冒泡排序：(假设后面元素有序)从头开始两两比对，前面的数字比后面大的话就调换，放在最后
    2. 选择排序：(假设前面元素有序) 从无序元素到最后，找到一个最小的元素min，放到有序元素后面
    3. 插入排序：(假设前面元素有序) 遍历无序元素，把每一个插到前面的相应位置，前面有序的元素需要向后挪动腾出位置
2. 平均O(nlogn)
    1. 归并排序：分治法
    2. 快速排序：分治法
    3. 堆排序（建立最大堆nlogn；(假设后面元素有序)把顶点放到heap后面，把无序的最后一个元素放到顶点并调整nlogn）
3. 近似O(n)
    1. 计数排序：n个数字，范围在0到k，开k+1个bucket去存储
    2. 桶排序：计数排序的升级版
    3. 基数排序：找到最大值的位数O(logmax_v)，从个位开始排序O(n)，一共O(nlogmax_v)
